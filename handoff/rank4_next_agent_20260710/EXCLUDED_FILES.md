# Deliberately excluded material

The archive is curated, not a byte-for-byte workspace dump.

Excluded as stale or unsafe:

- rank4_grothendieck_push.md;
- HANDOFF_NEXT_AGENT_2026-07-10.md and older session handoffs;
- all prior handoff zip versions and nested archives;
- THEORY_rank8.md and larger-rank work unrelated to the current request;
- prepared F4 task-16/task-17 launch packages, now theorem-retired;
- full old principal/F4 arrays containing retired multiplicative rows;
- slurm/verify_m2.sbatch, which only prints a version;
- response notes with known false explanations.

Excluded as non-evidence:

- partial and incomplete logs, except the two explicitly labeled inherited
  M2 snapshots;
- OOM/crash artifacts and core dumps;
- live partial principal logs;
- process outputs lacking terminal markers;
- duplicate old stretched/H1221 canaries not selected by their auditors.

Excluded for hygiene:

- virtual environments and Z3/M2 installations;
- Apptainer images;
- credentials, SSH state, and RCC home files;
- pycache, pyc, DS_Store, AppleDouble, and temporary files;
- unrelated old archives and large duplicate bundles.

The 103 principal logs included here are the immutable terminal logs selected
at freeze. Active partials were omitted.
