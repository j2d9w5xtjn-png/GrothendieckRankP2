# Exact \(S'\) sweep on the seven stretched length-five quotients

Date: 2026-07-10

## Terminal verdict

The exact search is complete on all seven rings in Section 3 of
`STRUCTURAL_LENGTH6_QUOTIENTS_F2_2026-07-10.md`, using all six
\(\mathbf F_2\)-rational \(xy\) Hopf fibers and all four \(t^4\) normal
forms.

There are \(7\cdot(6+4)=70\) base/fiber rows:

* 15 rows are **H0-vacuous**: the pinned residue Hopf fiber has no lift to a
  rank-four bialgebra over that base satisfying the fiber2 condition;
* 55 rows are nonvacuous, with H0 SAT and the \(S'\)-holding sanity query S1
  SAT;
* on every one of those 55 rows, each of the three split exact queries
  `FAIL_1`, `FAIL_2`, and `FAIL_3` is **UNSAT**;
* there are no SAT \(S'\)-failure rows and no unknown/time-out rows.

Thus \(S'\) is universal over all seven stretched length-five rings.  No SAT
seed exists in this complete search scope.  The script contains an
independent concrete seed validator, but it was not invoked because no
`FAIL_i` query was SAT.

These are \(S'\) verdicts, not direct \([4]\) verdicts.  Their downstream use
is through the socle-extension theorem.

## Exact scope

The bases and natural maximal-ideal generators used were:

| key | ring | generators |
|---|---|---|
| `B_F2x4` | \(\mathbf F_2[x,y]/(x^4,y^2,xy)\) | \((x,y)\) |
| `B_Z16` | \(\mathbf Z/16[y]/(2y,y^2)\) | \((2,y)\) |
| `B_quad_c0` | \(B(\mathbf Z/4[x]/(x^2-2))\) | \((x,y)\) |
| `B_quad_c1` | \(B(\mathbf Z/4[x]/(x^2-2x-2))\) | \((x,y)\) |
| `B_cubic` | \(B(\mathbf Z/4[x]/(x^3-2,x^4))\) | \((x,y)\) |
| `Q0` | \(\mathbf Z/4[x]/(x^4,2x)\) | \((2,x)\) |
| `Q1` | \(\mathbf Z/4[x]/(x^4,2x-x^3)\) | \((2,x)\) |

The six \(xy\) pins were

```text
a2a2, W2F, mu2mu2, mu2a2,
mu2mu2_unipotent, mu2mu2_irreducible.
```

The four \(t^4\) normal forms were

```text
(c1,c4) = (0,0), (0,1), (1,0), (1,1).
```

All omitted residue coproduct coefficients are pinned to zero and every
higher maximal-ideal deformation digit remains free.

## Gates and exact failure encoding

Before any Hopf query, each explicit 32-element ring passed:

1. the complete addition and multiplication tables, including exhaustive
   associativity and distributivity;
2. locality and the exact maximal ideal generated by the displayed pair;
3. the filtration sizes

   \[
   |\mathfrak m|,|\mathfrak m^2|,|\mathfrak m^3|,|\mathfrak m^4|
   =16,4,2,1;
   \]
4. its displayed presentation relations;
5. the exact four-element socle and
   \(\operatorname{Soc}(R)\mathfrak m=0\);
6. exhaustive enumeration of all \(32^2=1024\) coefficient pairs in the
   full syzygy module.

For every ring,

\[
 |\operatorname{Syz}(g_1,g_2)|=64,\qquad
 |\operatorname{Soc}(R)^2|=16,\qquad
 |\operatorname{Syz}/\operatorname{Soc}(R)^2|=4.
\]

The residual quotient is \(C_2\times C_2\) in six rows and \(C_4\) for
`B_Z16`.  This last point matters: an implementation that assumes an
\(\mathbf F_2\)-basis for every residual quotient is not exact on `B_Z16`.
The final script instead exhaustively derives the four cosets in every row.

The fiber2 equations put every entry of \(\phi=[2]^\#\) in \(\mathfrak m\).
Consequently socle shifts of division coefficients do not alter either the
division equation or kernel membership.  Each `FAIL_i` query is therefore
unrolled over the complete

\[
 4^3=64
\]

choices of residual syzygy coset in the three augmentation-ideal
coordinates.  The result is quantifier-free and exact.

## Row-by-row classification

| base | H0-vacuous fibers | nonvacuous fibers | exact \(S'\)-failure result |
|---|---|---:|---|
| `B_F2x4` | none | all 10 | all three `FAIL_i` UNSAT |
| `B_Z16` | `xy/a2a2`, `xy/W2F`, `xy/mu2a2`, `t4/(0,0)`, `t4/(1,0)` | other 5 | all three `FAIL_i` UNSAT |
| `B_quad_c0` | none | all 10 | all three `FAIL_i` UNSAT |
| `B_quad_c1` | none | all 10 | all three `FAIL_i` UNSAT |
| `B_cubic` | none | all 10 | all three `FAIL_i` UNSAT |
| `Q0` | `xy/a2a2`, `xy/W2F`, `xy/mu2a2`, `t4/(0,0)`, `t4/(1,0)` | other 5 | all three `FAIL_i` UNSAT |
| `Q1` | `xy/a2a2`, `xy/W2F`, `xy/mu2a2`, `t4/(0,0)`, `t4/(1,0)` | other 5 | all three `FAIL_i` UNSAT |

Totals: 15 H0-vacuous rows, 55 nonvacuous universally \(S'\) rows, zero
SAT failures, zero unknowns.

## Reproducibility artifacts

Runner:

```text
scripts/sprime_stretched_length5_stratified_20260710.py
```

Terminal logs:

```text
scripts/sprime_stretched_B_F2x4_20260710.log
scripts/sprime_stretched_B_Z16_20260710.log
scripts/sprime_stretched_B_quad_c0_20260710.log
scripts/sprime_stretched_B_quad_c1_20260710.log
scripts/sprime_stretched_B_cubic_20260710.log
scripts/sprime_stretched_Q0_20260710.log
scripts/sprime_stretched_Q1_20260710.log
```

Each log terminates with `DONE sprime_stretched_length5_stratified_20260710`
and a per-row summary.  The audit found exactly 70 row headers, 165 split
UNSAT verdicts ((55\cdot3)), 15 H0-vacuous markers, no traceback, no gate
failure, no SAT `FAIL_i`, and no unknown verdict.
